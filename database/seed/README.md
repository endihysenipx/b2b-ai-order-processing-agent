# Database Seed

Seed data is implemented in `backend/app/db/seed.py` and creates development users, clients, emails, orders, items, attachments, validation issues, generated XML records, and feedback issues.
